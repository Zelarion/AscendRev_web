/**
 * Was a 32KB base64 data URI embedded directly in this file, which shipped
 * the image inside the JS bundle instead of letting the browser cache it as
 * an asset. Decoded back to a real file at
 * `public/images/solutions-hero.webp` (960x540 WEBP) and referenced here by
 * path instead.
 */
export const solutionsHeroImage = '/images/solutions-hero.webp';
